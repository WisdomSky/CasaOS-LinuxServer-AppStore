name: linuxserver-winegui
services:
  app:
    image: linuxserver/winegui:4.4.0
    restart: unless-stopped
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    x-casaos:
      envs:
        - container: PUID
          description:
            en_US: Run 'id [USER]' for the owner of the host volume directories to get the UID to use here.
        - container: PGID
          description:
            en_US: Run 'id [USER]' for the owner of the host volume directories to get the GID to use here.
        - container: TZ
          description:
            en_US: specify a timezone to use, see this list.
    ports:
      - target: 3000
        published: 3000
        protocol: tcp
      - target: 3001
        published: 3001
        protocol: tcp
    volumes:
      - type: bind
        source: /DATA/AppData/winegui/config
        target: /config
x-casaos:
  architectures:
    - amd64
  main: app
  description:
    en_US: WineGUI is a user-interface friendly Wine manager that provides a graphical frontend for creating and managing Wine bottles.
    zh_CN: WineGUI 是一款界面友好的 Wine 管理器，提供图形前端以创建和管理 Wine 容器环境（bottles）。
  tagline:
    en_US: A user-interface friendly Wine manager that provides a graphical frontend for creating and managing Wine bottles.
    zh_CN: 通过图形界面创建和管理 Wine 环境。
  developer: LinuxServer.io
  author: WisdomSky
  icon: https://raw.githubusercontent.com/linuxserver/docker-templates/master/linuxserver.io/img/winegui-logo.png
  thumbnail: https://raw.githubusercontent.com/linuxserver/docker-templates/master/linuxserver.io/img/winegui-logo.png
  title:
    en_US: Winegui (LinuxServer.io)
    zh_CN: Winegui（LinuxServer.io）
  category: Home
  id: wisdomsky.linuxserver.linuxserverwinegui
  port_map: '3000'
  version: 4.4.0
