name: linuxserver-citron
services:
  app:
    image: linuxserver/citron:2026.02.1
    restart: unless-stopped
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
    x-casaos:
      envs:
        - container: PUID
          description:
            en_US: Run 'id [USER]' for the owner of the host volume directories to get the UID to use here.
        - container: PGID
          description:
            en_US: Run 'id [USER]' for the owner of the host volume directories to get the GID to use here.
        - container: TZ
          description:
            en_US: specify a timezone to use, see this list.
    ports:
      - target: 3000
        published: 3000
        protocol: tcp
      - target: 3001
        published: 3001
        protocol: tcp
    volumes:
      - type: bind
        source: /srv/lsio/citron/config
        target: /config
x-casaos:
  architectures:
    - amd64
  main: app
  description:
    en_US: Citron is a feature rich Switch 1 emulator for homebrew.
    zh_CN: Citron 是一款面向自制软件、功能丰富的 Switch 1 模拟器。
  tagline:
    en_US: A feature rich Switch 1 emulator for homebrew.
    zh_CN: 面向自制软件、功能丰富的 Switch 1 模拟器。
  developer: LinuxServer.io
  author: WisdomSky
  icon: https://raw.githubusercontent.com/linuxserver/docker-templates/master/linuxserver.io/img/citron-logo.png
  thumbnail: https://raw.githubusercontent.com/linuxserver/docker-templates/master/linuxserver.io/img/citron-logo.png
  title:
    en_US: Citron
    zh_CN: Citron
  category: Others
  id: wisdomsky.linuxserver.linuxservercitron
  version: 2026.02.1
